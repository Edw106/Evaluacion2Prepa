#include<stdio.h>
#include"message.h"


//Este será el main de prueba
int main()
{	
	Logger* logger = create_log(); //Crea el logger que utilizaremos
	
	Message prueba;
	char string[]
	
	add_character(prueba.string, prueba.size, )
	

	liberate_memory(logger); //Libera la memoria que ocupaba el logger
	return 0;
}
